# 2026-09-23: owner-authorized staged preparation amendment

## Subject and authority

Starting upstream main: `037e6513f882b25512928aab5883f8545b58fc84`, merged PR #13.
No open follow-up PR was present when inspected. The owner explicitly approved
new sequencing, reported hardware, requested Expo previews and delegated target
selection; [the retained statement](../prep/after-pr-10/owner-instruction-2026-09-23.md)
is an assistant transcription of the actual message, not an invented approval.

This is a normative gate amendment, NOT a dependency-only blocker refresh.
It does not assert whole-packet R0 adoption, independent approval, hardware
inspection, candidate implementation, physical measurements or production access.
The historical R0 proposal at `9567117404f635c803373d9957050fd8ec50f334` is unchanged;
acceptance for new A1 must explicitly cover the amended snapshot. Neither branch
publication nor merge creates that acceptance.

## Bounded changes

A1 requires accepted amended contract, seven exact inputs, identified authors,
independent oracle review, actual execution-host/Node/TypeScript/Elixir/full-OTP
versions and a retained replayable execution bundle with independent setup review.
Unknown native/phone fields may remain null. A2 requires the complete native and
physical bundle, approved common host and a fresh stage-specific setup review.
Default `--require-ready` remains full A2; explicit A1 success has limited output.

Existing schema 1, manifest/review fields and the permanent template are reused.
A2 keeps its original digest algorithm. A1 adds domain separation with the exact
UTF-8 prefix `loka-r1-a1-setup-v1` and NUL byte. Tests challenge cross-stage
receipt reuse even with full inventory, not merely rejection of missing devices.
Stage change or field change requires genuine new review, never just rehashing.

Under delegated target choice, iPhone 11/4 GB replaces SE 2/3 GB for initial iOS
qualification before any measurements. This narrows the claimed support class;
it does not show that the prior 3 GB device passes. The Android A14/4 GB target
remains until the old Pixel is inventoried and substitution reviewed. Palma 2 is
supplementary e-paper usability, not ordinary-phone timing or a new platform.
Owner-reported Mac model/16 GB and iPhone model/availability source are retained;
actual RAM/OS/build/SKU, cores and installed runtimes remain unverified where null.

Expo previews/emulators are development aids. A1 remains headless stateless
semantics; Hermes, Port integration, SQLite and physical release evidence remain
A2+. No app, native dependency update or gameplay is introduced.

## Preservation and intentional rebinding

Previous envelope SHA-256:
`c404127e08ed1965bad6cece6f707b45641d787791edc650d056a67baa6ea345`.
This authorized stage/qualification amendment changes only that input among the
11 preservation entries. The other ten, including every original gameplay,
numeric, composition, Lantern, capability/scope and blank-template byte, stay
fixed. All seven input paths/order remain; six semantic input hashes are unchanged.
The new envelope hash is recorded in `spec_tools/preserved-inputs.json` and the
pending setup/oracle records, not regenerated from candidate outputs.

The JavaScript dependency bundle, all nine SHA256SUMS entries and lock hash
`18ac7c91f854dcd55480dea15b57c52afa0d674863ce4d620221926b99644359` are unchanged.
No fresh npm replay, audit, native build or actual-host test is claimed.
Only pending digest/reference bindings are refreshed; no approval is renewed.

## Execution provenance

Fresh local baseline: 98 Python tests passed; scope/navigation/template checks
passed; blank and actual setup each returned readiness exit 1. Current container
has no Mix/Elixir/OTP; attempted Mix command returned 127. GitHub DNS failed, so
source was recovered from the retained exact-main CI artifact, ZIP SHA-256
`847a6427cbb2b4d0c8467cf8a485a0d44434e6b121b582398bc547865266f97d`, with exact source
marker checked. It is a scoped archive, not a claimed successful full clone.
Root AGENTS.md was read; no nested AGENTS.md occurs in its docs/workflow scope.

After checker/test edits, 107 Python tests passed (98 retained plus nine new
stage tests). Final source checks, formatting and ExUnit execution are recorded
in the PR's exact-head CI/provenance report; do not infer a hosted pass from this
pre-publication source record. The specification tooling pins remain unchanged.
Local Git metadata identifies a snapshot, not the upstream commit. Publication
must preserve the actual upstream parent and exclude any transport-only files
or workflow ancestry; no local network push is claimed without execution.

## Self-review and adversarial self-review

One implementing ChatGPT assistant performed both passes; neither is independent
oracle/setup approval and neither signs Fable's identity. The nominated reviewer
gets a bounded [prompt](../prep/after-pr-10/fable-review-prompt.md), not an approval.

Challenges include stage-default preservation, unsupported stages, incomplete
actual/template rejection at both stages, required A1 runtime/contract/authorship,
malformed deferred values, reference tampering, stage-bound hash separation,
A2 native requirements and exact iOS-class amendment. A complete A1-approved
synthetic record must still fail A2 due to the digest, even when no fields are
missing. Existing self-review/subject-author and numeric tests remain.

Known limitations: structural checks cannot authenticate people or device use,
prove complete native lock provenance, or establish mobile/SQLite/Port/performance
correctness. The prior scheduler cancellation/rescheduling and save-fork/restore
model coverage limitations remain. R1 numerical gates, A-first/B/C failure order,
PREP-03 at A5/R2 and the 57-room/ten-quest/two-ending release remain unchanged.
